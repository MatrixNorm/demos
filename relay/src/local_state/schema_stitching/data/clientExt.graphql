directive @local on FIELD

extend type Mutation {
  updateSelectedContinent(continent: Continent!): LocalSettings
}

extend type Query {
  localSettings: LocalSettings!
}

type LocalSettings {
  allContinents: [Continent!]!
  selectedContinent: Continent!
}
