extend type Query {
  localState: LocalState!
}

extend type PostOrdering {
  fieldDescription_ASC: String!
  fieldDescription_DESC: String!
}

type LocalState {
  postListingState(id: ID!) PostListingState
}

type PostListingState implements Node {
  id: ID!
  activeField: PostOrderingFields
  allOrderings: [PostOrdering!]!
}