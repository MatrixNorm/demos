import * as React from "react";
import { CloseCrossIcon } from "../elements/Icons";

export default { title: "cities_app-demo1/Icons" };

export const closeCross = () => {
  return <CloseCrossIcon />;
};
